# 2026-08-12 工作日志

## email-analyzer 技能实际用法（本工作区）
- `agent-mail` 连接器**未开通**（返回 not_bound），不能直接用 MCP 邮件工具。
- 实际走 **Foxmail/QQ 邮箱 IMAP + SMTP**：账户 ro_ye@foxmail.com（授权码在技能 email_config.py / 用户本会话提供）。
  - IMAP: imap.qq.com:993 SSL；SMTP: smtp.qq.com:465 SSL。
  - 技能自带 check_email.py / check_email_imap.py 写死 Gmail，需改用 QQ 配置。
- 分析邮件回复固定收件人：**sylvia.tan@assaabloy.com**（ASSA ABLOY，谭斯雅）。
- 输出目录：`cc-worker/email_analysis/`；环境用隔离 venv（openpyxl/pandas/matplotlib/reportlab）。

## 本次分析：Mexicali CVR 锁盖订单量（2025 vs 2026）
- 数据来源附件 `锁盖分析 08 12 26.xlsx`，3 个料号按月销量：
  - **27639-003** 单独分析：总量 13,641,000 → 15,180,000，**+11.3%**；CV 79.6%→59.4%（更平稳）。
  - **63251-002 + 63252-002 合并** 分析：总量 3,344,400 → 2,847,000，**-14.9%**。
- 数据缺口：63251-002 的 2026 年 9 月为空白（NaN），计算时剔除并标注 N/A。
- 交付物：Excel（Summary / 27639_Monthly / Combined_Monthly / Fluctuation_3SKU / Raw_Data）+ PDF（关键发现 + 柱状图 + 原因解读），已邮件回复 Sylvia。
- 图表标题用**英文**避免中文字体缺失（matplotlib 无 CJK 字体）；附件名用英文防邮件客户端显示 noname。
- 邮件发送附件必须 `email.encoders.encode_base64`，否则 `as_string()` 报 UnicodeEncodeError(\ufffd)。
